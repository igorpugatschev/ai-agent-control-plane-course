diff --git a/src/task_app/models.py b/src/task_app/models.py
index 5100484..fdfed84 100644
--- a/src/task_app/models.py
+++ b/src/task_app/models.py
@@ -9,0 +10,6 @@ class TaskStatus(StrEnum):
+class TaskPriority(StrEnum):
+    HIGH = "high"
+    NORMAL = "normal"
+    LOW = "low"
+
+
@@ -14,0 +21 @@ class Task:
+    priority: TaskPriority = TaskPriority.NORMAL
diff --git a/src/task_app/service.py b/src/task_app/service.py
index 5082b02..40f510c 100644
--- a/src/task_app/service.py
+++ b/src/task_app/service.py
@@ -1 +1 @@
-from .models import Task, TaskStatus
+from .models import Task, TaskPriority, TaskStatus
@@ -9 +9,3 @@ class TaskService:
-    def create_task(self, title: str) -> Task:
+    def create_task(
+        self, title: str, priority: TaskPriority | str = TaskPriority.NORMAL
+    ) -> Task:
@@ -12,0 +15,4 @@ class TaskService:
+        try:
+            normalized_priority = TaskPriority(priority)
+        except ValueError as error:
+            raise ValueError("Недопустимый приоритет") from error
@@ -18 +24,5 @@ class TaskService:
-        task = Task(id=self._next_id, title=normalized)
+        task = Task(
+            id=self._next_id,
+            title=normalized,
+            priority=normalized_priority,
+        )
